I’ll review only `e38fd14..HEAD` and the specified paths, read the rev-2 plan and repository instructions first, then trace each of the 40 findings through implementation and tests. I’ll keep the workspace untouched.The scoped history is exactly the seven named commits, and the worktree is clean. I found no `AGENTS.md` or `CLAUDE.md` under the workspace via the initial search; I’m doing a hidden-file check, then reading the entire plan and its independent review before judging code.I now have all 40 defect scenarios, all four design decisions, all eight deliberate deviations, and both phase/verification requirements mapped. Next I’m tracing the daemon as a state machine against the previous revision, with special attention to response ownership and pre-response races.One concrete daemon gap has emerged: normal `turn/completed` still finalizes inline and never settles a parked or replay-buffered server request, despite rev 2 explicitly requiring settlement on the completion path. I’m now checking whether the shared loop or its migrated consumers introduce additional observable regressions.The shared loop’s deadline arithmetic itself is monotonic and avoids nonpositive wait timeouts, but I found a migrated-behavior regression: `onMalformedQuestion: 'return'` executes `break`, then the generic parked-state check treats it as drain exhaustion and interrupts. I’m validating the remaining daemon races and the tests that claim to cover them.The main functional findings are now confirmed with direct in-memory state probes: completion can leave a parked request unanswered, a second same-turn request overwrites the first, and a request arriving during cancellation re-parks the turn. I’m doing the final scoped consistency check and consolidating the vacuous-test list.Issues found:

- `codex-claude/lib/daemon.mjs:588` — `turn/completed` bypasses `_finalizeTurn`, directly setting `status`, `message`, and `finalized`, and never calls `_settleOutstandingRequests()`. A parked request therefore remains unanswered after terminal completion; failed/interrupted completions also violate the single-terminal-writer invariant — compute the terminal message/status, then route every completion through `_finalizeTurn`.

- `codex-claude/lib/daemon.mjs:653` — `_onServerRequest` ignores both an existing `turn.parked` and `cancelRequested`. A second request overwrites the first at line 662 without answering it, while a request arriving during known-ID interrupt re-parks the cancelling turn and can prevent completion — queue subsequent requests or immediately answer/decline them whenever another request is parked or cancellation is pending.

- `codex-claude/lib/daemon.mjs:535` — protocol-dependent: pre-response stale filtering remembers only one ID. A delayed completion from an older known turn—anything other than exactly `_lastTurnId`—still triggers I4 and falsely finalizes/quarantines the new turn — retain a bounded set of retired turn IDs and reject any matching pre-response completion.

- `codex-claude/lib/drive-loop.mjs:97` — `onMalformedQuestion: 'return'` does not return. It executes `break`, after which line 111 treats the still-parked question as drain exhaustion and interrupts with `status:'failed'`. Both migrated round scripts explicitly use this mode, so their previous behavior was not preserved — return `res` directly or distinguish malformed-return from guard exhaustion.

- `codex-claude/bin/codex-drive.mjs:169` — deviation 1’s fail-closed probe is only partially implemented. A timeout correctly refuses replacement, but it unconditionally calls `fail()` even with `--force`, despite its own error text and the approved mitigation saying `--force` is the escape hatch — let explicit force perform best-effort teardown/replacement, or remove that promised recovery from the contract.

- `codex-claude/scripts/commit-review-round.mjs:35` — the approved “help pre-scan stays first” rule is violated because `testWaitMs()` runs before argv inspection. With an ambient invalid `CODEX_DRIVE_TEST_WAIT_MS`, `--help` exits 1 with a stack trace instead of usage — initialize the wait cap after the help and argument preflight.

- `codex-claude/scripts/commit-review-round.mjs:173` — rev 2 required an explicit null check at every `fullSha` caller. This caller silently omits `head=` when `fullSha(cwd, 'HEAD')` returns null, potentially allowing an unattested clean exit — fail explicitly and route the failure through the script’s normal fatal handling.

- `codex-claude/lib/drive-loop.mjs:61` — the migrated drivers did not preserve byte-identical diagnostics: review/plan timeout wording changed, safe approvals no longer log the command, unsupported requests now add output, and commit-review changed “denying” to “declining” — parameterize policy-specific logging or preserve the previous consumer strings.

- `codex-claude/test/daemon-review.test.mjs:363` — several daemon regression tests are vacuous. The rejected-start test calls `_finalizeTurn` directly rather than exercising `_sendStart`’s rejection arm; the “BOTH arms” generation test at line 518 never invokes the rejection arm; the replay test at line 505 drops foreign/malformed entries before they enter the buffer; and the interrupt-order test at line 408 does not assert parked-null/status normalization — drive the real paths and assert the required intermediate state.

- `codex-claude/test/drive-loop.test.mjs:104` — the action-error test covers only `approve`, not `answer`; the malformed fail/return test at line 116 never tests malformed-return; and the total-deadline test at line 148 times out on its first per-wait cap, so it would pass if `deadlineMs` were ignored entirely. Replacing `performance.now()` with wall-clock time would also remain green — add distinct answer-error, malformed-return, multi-drain deadline, and injectable-clock tests.

- `codex-claude/test/round-scripts.test.mjs:82` — phase 5’s plan-round consumer coverage is incomplete: no plan approval-policy or static re-ask path is exercised. The signal test at line 101 only checks exit code and logging, so removing `daemon.stop()` while retaining the handler would still pass; it also performs no mock-process census — test plan approvals/retry output and compare mock process counts before/after.

- `codex-claude/test/detached-cli.test.mjs:101` — finding 15 remains unclosed: the `read --out` test constructs neither an absent global state nor a mismatched decoy state under a temporary `HOME`, so both required regressions remain unguarded — run the case table under controlled absent and mismatched state files.

- `codex-claude/test/detached-cli.test.mjs:78` — finding 38 is only partially fixed. This test starts its daemon directly rather than through `startPrivate`, so its socket is never added to `SPAWNED`; an assertion failure before the trailing stop still orphans it — register the socket immediately after startup or use a tracked helper for every start path.

- `codex-claude/test/daemon-review.test.mjs:559` — the new cwd-coherence fixture creates a `cdx-rec-` directory that is never registered for cleanup, leaking one directory per suite run — retain its parent path in `CREATED` or remove it in a `finally`/suite hook.

VERDICT: ISSUES FOUND
